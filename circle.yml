machine:
  xcode:
    version: '7.1'
  ruby:
    version: "2.0.0"
  environment:
    XCODE_WORKSPACE: "EPStackExample/EPStackExample.xcworkspace"
    LANG: en_US.UTF-8

dependencies:
  cache_directories:
    - "EPStackExample/Pods"
